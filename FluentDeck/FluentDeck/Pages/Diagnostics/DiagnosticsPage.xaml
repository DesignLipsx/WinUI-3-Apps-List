<Page
    x:Class="FluentDeck.Pages.Diagnostics.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24,24,24,0" RowDefinitions="Auto,Auto,*">
        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,16" ColumnDefinitions="*,Auto" ColumnSpacing="16">
            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Repo Health Center" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Audit and diagnostics tool to find formatting issues, broken URLs, and clean catalog listings." 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
            </StackPanel>
        </Grid>

        <!-- Summary & Progress Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,16" ColumnDefinitions="*,*,*,*" ColumnSpacing="12">
            <!-- Total Items Card -->
            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}" Padding="16,12">
                <StackPanel Spacing="4">
                    <TextBlock Text="Total Unique Apps" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock x:Name="TotalAppsTxt" Text="0" Style="{StaticResource TitleTextBlockStyle}" />
                </StackPanel>
            </Border>

            <!-- Formatting Issues Card -->
            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}" Padding="16,12">
                <StackPanel Spacing="4">
                    <TextBlock Text="Formatting Issues" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock x:Name="FormattingIssuesTxt" Text="0" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                </StackPanel>
            </Border>

            <!-- Bad URLs Card -->
            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}" Padding="16,12">
                <StackPanel Spacing="4">
                    <TextBlock Text="Dead / Warning URLs" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock x:Name="DeadUrlsTxt" Text="0" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                </StackPanel>
            </Border>

            <!-- Progress Card -->
            <Border Grid.Column="3" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}" Padding="16,12">
                <Grid RowDefinitions="*,Auto" RowSpacing="8">
                    <StackPanel Grid.Row="0" Spacing="4">
                        <TextBlock Text="Status" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" />
                        <TextBlock x:Name="ProgressStatusTxt" Text="Idle" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <ProgressBar x:Name="WorkProgressBar" Grid.Row="1" Minimum="0" Maximum="100" Value="0" IsIndeterminate="False" Visibility="Collapsed" />
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content Area using SelectorBar -->
        <Grid Grid.Row="2" RowDefinitions="Auto,*">
            <!-- Navigation Header for Page Sections -->
            <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,12">
                <SelectorBar x:Name="DiagnosticsSelectorBar" SelectionChanged="DiagnosticsSelectorBar_SelectionChanged" Grid.Column="0" AutomationProperties.AutomationId="DiagnosticsSelectorBar" Padding="0">
                    <SelectorBarItem x:Name="FormattingTab" Text="Formatting Issues" IsSelected="True" />
                    <SelectorBarItem x:Name="UrlTab" Text="URL Check Results" />
                </SelectorBar>

                <!-- Actions positioned on the right edge of SelectorBar -->
                <StackPanel x:Name="TabActionsPanel" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <!-- Formatting Actions -->
                    <StackPanel x:Name="FormattingActions" Orientation="Horizontal" Spacing="8">
                        <Button x:Name="CleanStoreUrlsBtn" Content="Clean MS Store URLs" Click="CleanStoreUrls_Click" ToolTipService.ToolTip="Removes query params like ?hl=en from Store URLs" AutomationProperties.AutomationId="CleanStoreUrlsButton" />
                        <Button x:Name="FixGitHubLogosBtn" Content="Fix GitHub Logo Links" Click="FixGitHubLogos_Click" ToolTipService.ToolTip="Changes github.com/.../blob/ to raw.githubusercontent.com" AutomationProperties.AutomationId="FixGitHubLogosButton" />
                        <Button x:Name="FixAllBtn" Click="FixAllBtn_Click" ToolTipService.ToolTip="Fixes common formatting alignment &amp; syntax errors" AutomationProperties.AutomationId="FixAllButton">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE777;" FontSize="14"/>
                                <TextBlock Text="Auto-Fix Formatting"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="ScanBtn" Click="ScanBtn_Click" Style="{StaticResource AccentButtonStyle}" AutomationProperties.AutomationId="ScanButton">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                <TextBlock Text="Scan Formatting"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- URL Actions -->
                    <StackPanel x:Name="UrlActions" Orientation="Horizontal" Spacing="8" Visibility="Collapsed">
                        <Button x:Name="CheckUrlsBtn" Click="CheckUrlsBtn_Click" Style="{StaticResource AccentButtonStyle}" AutomationProperties.AutomationId="CheckUrlsButton">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE774;" FontSize="14"/>
                                <TextBlock Text="Check URLs &amp; Logos"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Display Panels -->
            <Frame x:Name="ContentFrame" Grid.Row="1" IsNavigationStackEnabled="False" />
        </Grid>
    </Grid>
</Page>
