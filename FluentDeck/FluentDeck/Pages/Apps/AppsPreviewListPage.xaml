<Page
    x:Class="FluentDeck.Pages.Apps.AppsPreviewListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FluentDeck.Models"
    xmlns:local="using:FluentDeck.Pages.Apps"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <CollectionViewSource x:Name="CatalogCVS" IsSourceGrouped="True"/>

        <DataTemplate x:Key="AppTemplate" x:DataType="models:CatalogAppItem">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="{x:Bind local:AppsPage.AppMargin(HeadingLevel)}">
                <StackPanel.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Edit App Info..." Click="EditApp_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE70F;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </StackPanel.ContextFlyout>
                <TextBlock Text="•" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                <Border CornerRadius="4" Padding="6,2" Background="{ThemeResource ControlFillColorSecondaryBrush}" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind Indicator}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Border>
                <HyperlinkButton Content="{x:Bind Name}" NavigateUri="{x:Bind NavigateUri}" Padding="0" VerticalAlignment="Center" FontWeight="SemiBold" Style="{StaticResource DefaultHyperlinkButtonStyle}" />
                <Border CornerRadius="4" Padding="6,2" Background="{ThemeResource ControlFillColorSecondaryBrush}" Visibility="{x:Bind local:AppsPage.BoolToVisibility(IsPlanned)}" VerticalAlignment="Center">
                    <TextBlock Text="📆" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
                </Border>
                <Border CornerRadius="4" Padding="6,2" Background="{ThemeResource ControlFillColorSecondaryBrush}" Visibility="{x:Bind local:AppsPage.BoolToVisibility(IsFoss)}" VerticalAlignment="Center">
                    <TextBlock Text="FOSS" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Border>
                <Border CornerRadius="4" Padding="6,2" Background="{ThemeResource ControlFillColorSecondaryBrush}" Visibility="{x:Bind local:AppsPage.BoolToVisibility(IsPaid)}" VerticalAlignment="Center">
                    <TextBlock Text="💰" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
                </Border>
                <Border CornerRadius="4" Padding="6,2" Background="{ThemeResource ControlFillColorSecondaryBrush}" Visibility="{x:Bind local:AppsPage.BoolToVisibility(IsDiscontinued)}" VerticalAlignment="Center">
                    <TextBlock Text="❎" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <ProgressRing x:Name="LoadingRing"
                      IsActive="False"
                      Visibility="Collapsed"
                      Width="48" Height="48"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ListView x:Name="ReadmeListView" ItemsSource="{x:Bind CatalogCVS.View, Mode=OneWay}" ItemTemplate="{StaticResource AppTemplate}" SelectionMode="None" IsItemClickEnabled="False" Padding="0,8,16,16">
            <ListView.Resources>
                <Thickness x:Key="ListViewHeaderItemMargin">0</Thickness>
                <Thickness x:Key="ListViewHeaderItemPadding">0</Thickness>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0,8,0,4"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="MinHeight" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="False" Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderContainerStyle>
                        <Style TargetType="ListViewHeaderItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="MinHeight" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewHeaderItem">
                                        <ContentPresenter Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          ContentTransitions="{TemplateBinding ContentTransitions}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          Margin="0" Padding="0"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.HeaderContainerStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="models:GridAppGroup">
                            <StackPanel HorizontalAlignment="Stretch">
                                <Grid Margin="{x:Bind local:AppsPage.HeaderMargin(Header.Level)}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Click="HeaderButton_Click" Background="Transparent" BorderThickness="0" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.Name="{x:Bind Header.Text}">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <Image Source="{x:Bind local:AppsPage.PathToImageSource(Header.IconPath)}" Width="28" Height="28" Stretch="Uniform" UseLayoutRounding="True" VerticalAlignment="Center" Visibility="{x:Bind Header.IconVisibility}"/>
                                            <TextBlock Text="{x:Bind Header.Text}"
                                                        Style="{x:Bind local:AppsPage.HeaderTextStyle(Header.Level)}"
                                                        VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                    <Button Grid.Column="1" x:Name="AddAppButton" Click="AddAppButton_Click" VerticalAlignment="Center"
                                            Visibility="{x:Bind local:AppsPage.AddAppButtonVisibility(Header.IsLeafCategory)}" Padding="12,4"
                                            ToolTipService.ToolTip="{x:Bind Header.AddButtonToolTip}">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE109;" FontSize="12"/>
                                            <TextBlock Text="Add"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                                <!-- Full-Width Border line under section header -->
                                <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,8,8,4" Visibility="{x:Bind local:AppsPage.HeaderSeparatorVisibility(Header.Level)}" />
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</Page>
